## Ошибка
В условии использовался оператор присваивания (=) вместо сравнения (==), из-за чего код не запускался.

## Исправление
Заменён оператор `=` на `==` в условии проверки делимости.

## Результат
Программа корректно выводит FizzBuzz для чисел от 1 до 99.