## Ошибка
Цикл range(1, max_num) не включал последнее число max_num.

## Исправление
Изменён диапазон на range(1, max_num + 1), чтобы включить 99.

## Результат
Программа корректно выполняет FizzBuzz для всех чисел от 1 до 99 включительно.