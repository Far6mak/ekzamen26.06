## Ошибка
Функция fizzbuzz была вызвана без обязательного аргумента max_num, что приводило к TypeError.

## Исправление
Добавлен аргумент при вызове функции: fizzbuzz(99).

Также скорректирован range, чтобы включать верхнюю границу.